using System;

namespace Lesson20.Extensions
{
    public class DateTimeExtensions
    {
        // int GetDaysBetween(DateTime date1, DateTime date2)      -- gets days count
        // int CalculateAge(DateTime date)                         -- age
        // bool IsWorkingDay(DateTime date)
        // bool IsWeekend(DateTime date)
        // IEnumerable<T[]> ChunkBy(IEnumerable<T> enumerable, int size) *
        // private DateTime dateTime = new DateTime(12, 12, 1999);
         // dateTime.CalculateAge();
         // dateTime.CalculateAge(date2);
    }
}